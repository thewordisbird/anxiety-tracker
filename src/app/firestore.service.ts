import { Injectable } from '@angular/core';

import * as firebase from "firebase/app";
import "firebase/firestore";


@Injectable({
  providedIn: 'root'
})
/**
 * Service to handle firestore transaction
 * Post data to firestore from form
 * Get data from firestore for table
 */
export class FirestoreService {

  constructor() { }
}
